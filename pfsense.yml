- hosts: pfsense
  tasks:
  - name: "Add Internal DNS out rule"
    pfsense_rule:
      name: 'Allow Internal DNS traffic out'
      action: pass
      interface: lan
      ipprotocol: inet
      protocol: tcp
      source: "{{ vm_ip_address }}"
      destination: any:80
      state: present
      
